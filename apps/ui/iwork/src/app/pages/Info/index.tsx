import { WorkInProgress } from "@ui/ui-lib";
import { InfoContainer } from "./styles";

const InfoPage = () => {
  return (
    <InfoContainer>
      <WorkInProgress />
    </InfoContainer>
  );
};

export default InfoPage;
