// Generated by Copilot
import { styled } from "@mui/material/styles";
import { Box, Typography, ListItemButton } from "@mui/material";

export const DRAWER_WIDTH = 165;
export const DRAWER_COLLAPSED_WIDTH = 64;

export const HRPortalRoot = styled(Box)(() => ({
  minHeight: "100vh",
  width: "100%",
  display: "flex",
  justifyContent: "flex-start",
  alignItems: "flex-start",
  background: "linear-gradient(180deg, #f8fbff 0%, #edf5fd 100%)",
  padding: 0,
  boxSizing: "border-box",
  overflow: "hidden",
}));

export const HRDesignViewport = styled(Box)(() => ({
  position: "relative",
  display: "flex",
  width: "100%",
  height: "100vh",
  overflow: "hidden",
}));

export const SidebarContainer = styled(Box, {
  shouldForwardProp: (p) => p !== "collapsed",
})<{ collapsed: boolean }>(({ theme, collapsed }) => ({
  width: collapsed ? DRAWER_COLLAPSED_WIDTH : DRAWER_WIDTH,
  flexShrink: 0,
  backgroundColor: theme.palette.background.paper,
  borderRight: `1px solid ${theme.palette.neutral.tableBorder}`,
  display: "flex",
  flexDirection: "column",
  transition: "width 0.2s ease",
  overflow: "hidden",
  position: "relative",
  zIndex: 1,
  top: "auto",
  height: "100%",
  [theme.breakpoints.down("sm")]: {
    width: DRAWER_COLLAPSED_WIDTH,
  },
}));

export const SidebarHeader = styled(Box)(({ theme }) => ({
  display: "flex",
  alignItems: "center",
  justifyContent: "space-between",
  padding: theme.spacing(2, 2),
  borderBottom: `1px solid ${theme.palette.neutral.tableBorder}`,
  minHeight: 56,
}));

export const SidebarLogo = styled(Typography)(({ theme }) => ({
  fontWeight: theme.typography.fontWeights.semiBold,
  fontSize: theme.typography.fontSizes.sm,
  color: theme.palette.neutral.medium,
  whiteSpace: "nowrap",
  overflow: "hidden",
}));

export const SidebarNavSection = styled(Box)(({ theme }) => ({
  padding: theme.spacing(1, 1.5),
  display: "flex",
  flexDirection: "column",
  gap: 2,
  flexGrow: 1,
  overflowY: "auto",
}));

export const SidebarNavItem = styled(ListItemButton, {
  shouldForwardProp: (p) => p !== "active" && p !== "collapsed",
})<{ active?: boolean; collapsed?: boolean }>(
  ({ theme, active, collapsed }) => ({
    borderRadius: theme.shape.borderRadii.medium,
    padding: collapsed ? theme.spacing(1.25) : theme.spacing(1, 1.5),
    justifyContent: collapsed ? "center" : "flex-start",
    gap: theme.spacing(1.5),
    minHeight: 40,
    color: active ? theme.palette.secondary.selected : theme.palette.text.grey,
    backgroundColor: active
      ? theme.palette.background.tableHeader
      : "transparent",
    fontWeight: active
      ? theme.typography.fontWeights.semiBold
      : theme.typography.fontWeights.regular,
    "&:hover": {
      backgroundColor: theme.palette.background.tableHeader,
      color: theme.palette.neutral.mediumDark,
    },
    transition: "all 0.15s ease",
  })
);

export const NavItemLabel = styled(Typography)(({ theme }) => ({
  fontSize: theme.typography.fontSizes.xss,
  fontWeight: "inherit",
  color: "inherit",
  whiteSpace: "nowrap",
}));

export const HRMainContent = styled(Box)(() => ({
  flex: 1,
  display: "flex",
  flexDirection: "column",
  minWidth: 0,
  overflow: "hidden",
}));

export const HRDesignFrame = styled(Box)(() => ({
  width: "100%",
  height: "100%",
  display: "flex",
  flexDirection: "row",
  fontFamily: "Figtree, sans-serif",
  overflow: "hidden",
  borderRadius: 0,
  background: "#EBF6FF",
}));

export const HRTopBar = styled(Box)(({ theme }) => ({
  height: 64,
  backgroundColor: "#ffffff",
  borderBottom: "1px solid #e4ebf3",
  display: "flex",
  alignItems: "center",
  justifyContent: "space-between",
  padding: theme.spacing(0, 3),
  gap: theme.spacing(2),
  zIndex: 100,
  flexShrink: 0,
}));

export const HRTopBarTitle = styled(Typography)(({ theme }) => ({
  fontSize: theme.typography.fontSizes.md,
  fontWeight: theme.typography.fontWeights.semiBold,
  color: theme.palette.neutral.mediumDark,
}));

export const HRPageContent = styled(Box)(({ theme }) => ({
  flex: 1,
  background: "#EBF6FF",
  paddingTop: 0,
  paddingLeft: theme.spacing(3),
  paddingRight: theme.spacing(3),
  paddingBottom: theme.spacing(3),
  overflowY: "auto",
  overflowX: "hidden",
  [theme.breakpoints.down("sm")]: {
    paddingTop: 0,
    paddingLeft: theme.spacing(1.5),
    paddingRight: theme.spacing(1.5),
    paddingBottom: theme.spacing(2),
  },
}));

export const KPIGrid = styled(Box)(({ theme }) => ({
  display: "grid",
  gridTemplateColumns: "repeat(auto-fit, minmax(200px, 1fr))",
  gap: theme.spacing(3.5),
  marginBottom: theme.spacing(4),
}));

export const KPICard = styled(Box)(({ theme }) => ({
  backgroundColor: theme.palette.background.paper,
  borderRadius: theme.shape.borderRadii.normal,
  padding: theme.spacing(3),
  boxShadow: theme.shadows[1],
  border: `1px solid ${theme.palette.neutral.tableBorder}`,
}));

export const KPILabel = styled(Typography)(({ theme }) => ({
  fontSize: theme.typography.fontSizes.xs,
  color: theme.palette.text.grey,
  fontWeight: theme.typography.fontWeights.medium,
  textTransform: "uppercase",
  letterSpacing: "0.04em",
}));

export const KPIValue = styled(Typography)(({ theme }) => ({
  fontSize: theme.typography.fontSizes.xll,
  fontWeight: theme.typography.fontWeights.semiBold,
  color: theme.palette.neutral.mediumDark,
  lineHeight: 1.2,
  marginTop: theme.spacing(0.75),
  marginBottom: theme.spacing(0.75),
}));

export const KPIChange = styled(Typography, {
  shouldForwardProp: (p) => p !== "positive",
})<{ positive?: boolean }>(({ theme, positive }) => ({
  fontSize: theme.typography.fontSizes.xs,
  color: positive ? theme.palette.neutral.green : theme.palette.text.error,
  fontWeight: theme.typography.fontWeights.medium,
  display: "flex",
  alignItems: "center",
  gap: 2,
}));

export const SectionCard = styled(Box)(({ theme }) => ({
  backgroundColor: theme.palette.background.paper,
  borderRadius: theme.shape.borderRadii.normal,
  padding: theme.spacing(4),
  boxShadow: theme.shadows[1],
  border: `1px solid ${theme.palette.neutral.tableBorder}`,
  marginBottom: theme.spacing(4),
}));

export const SectionTitle = styled(Typography)(({ theme }) => ({
  fontSize: theme.typography.fontSizes.sm,
  fontWeight: theme.typography.fontWeights.bold,
  color: theme.palette.neutral.mediumDark,
  marginBottom: theme.spacing(2),
}));

export const StyledTable = styled("table")(({ theme }) => ({
  width: "100%",
  borderCollapse: "collapse",
  fontFamily: "Figtree, sans-serif",
  fontSize: 15,
}));

export const StyledTh = styled("th")(({ theme }) => ({
  padding: theme.spacing(1.75, 2),
  textAlign: "left" as const,
  backgroundColor: "#4B6B8A",
  color: "#fff",
  fontWeight: theme.typography.fontWeights.semiBold,
  fontFamily: "Figtree, sans-serif !important",
  fontSize: "15px !important",
  lineHeight: "1.5 !important",
  borderBottom: "1px solid rgba(255,255,255,0.15)",
}));

export const StyledTd = styled("td")(({ theme }) => ({
  padding: theme.spacing(2.25, 2),
  borderBottom: `1px solid ${theme.palette.neutral.tableBorder}`,
  color: theme.palette.neutral.medium,
  fontFamily: "Figtree, sans-serif !important",
  fontSize: "15px !important",
  lineHeight: "1.5 !important",
}));

export const StatusBadge = styled(Box, {
  shouldForwardProp: (p) => p !== "status",
})<{ status?: string }>(({ theme, status }) => {
  const map: Record<string, { bg: string; color: string }> = {
    Active: {
      bg: theme.palette.chips.primary,
      color: theme.palette.neutral.green,
    },
    Inactive: {
      bg: theme.palette.chips.secondary,
      color: theme.palette.text.error,
    },
    Pending: {
      bg: theme.palette.chips.ternary,
      color: theme.palette.text.yellow,
    },
    Approved: {
      bg: theme.palette.chips.primary,
      color: theme.palette.neutral.green,
    },
    Rejected: {
      bg: theme.palette.chips.secondary,
      color: theme.palette.text.error,
    },
    Processing: {
      bg: theme.palette.chips.quaternary,
      color: theme.palette.secondary.selected,
    },
    Low: {
      bg: theme.palette.chips.primary,
      color: theme.palette.neutral.green,
    },
    Medium: {
      bg: theme.palette.chips.ternary,
      color: theme.palette.text.yellow,
    },
    High: {
      bg: theme.palette.chips.secondary,
      color: theme.palette.text.error,
    },
  };
  const s = status
    ? map[status] ?? {
        bg: theme.palette.background.tableHeader,
        color: theme.palette.neutral.medium,
      }
    : {
        bg: theme.palette.background.tableHeader,
        color: theme.palette.neutral.medium,
      };
  return {
    display: "inline-flex",
    alignItems: "center",
    padding: "2px 10px",
    borderRadius: 20,
    fontSize: theme.typography.fontSizes.xxs,
    fontWeight: theme.typography.fontWeights.semiBold,
    backgroundColor: s.bg,
    color: s.color,
    whiteSpace: "nowrap" as const,
  };
});

export const TabBar = styled(Box)(({ theme }) => ({
  display: "flex",
  alignItems: "center",
  borderBottom: `1px solid ${theme.palette.neutral.tableBorder}`,
  marginBottom: theme.spacing(3),
  marginLeft: theme.spacing(-3),
  marginRight: theme.spacing(-3),
  paddingLeft: theme.spacing(3),
  paddingRight: theme.spacing(3),
  gap: 0,
  overflowX: "auto",
  backgroundColor: theme.palette.background.paper,
  position: "sticky",
  top: 0,
  zIndex: 10,
}));

export const TabItem = styled(Box, {
  shouldForwardProp: (p) => p !== "active",
})<{ active?: boolean }>(({ theme, active }) => ({
  display: "flex",
  alignItems: "center",
  gap: theme.spacing(1),
  padding: theme.spacing(1.25, 2),
  cursor: "pointer",
  borderBottom: active
    ? `2px solid ${theme.palette.secondary.selected}`
    : "2px solid transparent",
  color: active ? theme.palette.secondary.selected : theme.palette.text.grey,
  fontWeight: active
    ? theme.typography.fontWeights.semiBold
    : theme.typography.fontWeights.regular,
  fontSize: theme.typography.fontSizes.xss,
  whiteSpace: "nowrap" as const,
  transition: "all 0.15s ease",
  "&:hover": {
    color: theme.palette.neutral.medium,
  },
}));

export const SearchBox = styled(Box)(({ theme }) => ({
  display: "flex",
  alignItems: "center",
  gap: theme.spacing(1),
  backgroundColor: theme.palette.background.tableHeader,
  border: `1px solid ${theme.palette.neutral.tableBorder}`,
  borderRadius: theme.shape.borderRadii.medium,
  padding: theme.spacing(0.75, 1.5),
  "& input": {
    border: "none",
    outline: "none",
    backgroundColor: "transparent",
    fontSize: theme.typography.fontSizes.xss,
    color: theme.palette.neutral.medium,
    width: "100%",
  },
}));

export const ActionButton = styled(Box)(({ theme }) => ({
  display: "inline-flex",
  alignItems: "center",
  gap: theme.spacing(0.75),
  padding: theme.spacing(0.75, 1.75),
  borderRadius: theme.shape.borderRadii.medium,
  fontSize: theme.typography.fontSizes.xss,
  fontWeight: theme.typography.fontWeights.medium,
  cursor: "pointer",
  border: `1px solid ${theme.palette.neutral.tableBorder}`,
  backgroundColor: theme.palette.background.paper,
  color: theme.palette.neutral.medium,
  "&:hover": {
    backgroundColor: theme.palette.background.tableHeader,
  },
  transition: "background-color 0.15s ease",
}));

export const PrimaryButton = styled(Box)(({ theme }) => ({
  display: "inline-flex",
  alignItems: "center",
  gap: theme.spacing(0.75),
  padding: theme.spacing(0.75, 1.75),
  borderRadius: theme.shape.borderRadii.medium,
  fontSize: theme.typography.fontSizes.xss,
  fontWeight: theme.typography.fontWeights.medium,
  cursor: "pointer",
  backgroundColor: theme.palette.button.primaryBlue,
  color: theme.palette.neutral.veryLight,
  "&:hover": {
    backgroundColor: theme.palette.button.primaryBlueHover,
  },
  transition: "background-color 0.15s ease",
}));

export const ChartBox = styled(Box)(({ theme }) => ({
  backgroundColor: theme.palette.background.paper,
  borderRadius: theme.shape.borderRadii.normal,
  padding: theme.spacing(3),
  boxShadow: theme.shadows[1],
  border: `1px solid ${theme.palette.neutral.tableBorder}`,
}));

export const TwoColGrid = styled(Box)(({ theme }) => ({
  display: "grid",
  gridTemplateColumns: "1fr 1fr",
  gap: theme.spacing(2.5),
  [theme.breakpoints.down("md")]: {
    gridTemplateColumns: "1fr",
  },
}));

export const ThreeColGrid = styled(Box)(({ theme }) => ({
  display: "grid",
  gridTemplateColumns: "1fr 1fr 1fr",
  gap: theme.spacing(2.5),
  [theme.breakpoints.down("md")]: {
    gridTemplateColumns: "1fr",
  },
}));
