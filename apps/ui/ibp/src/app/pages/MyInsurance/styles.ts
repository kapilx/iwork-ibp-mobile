import { Box, Typography } from '@mui/material';
import { styled } from '@mui/material/styles';

export const MyInsuranceContainer = styled(Box)(({ theme }) => ({
   paddingTop: theme.spacing(15),
}));
