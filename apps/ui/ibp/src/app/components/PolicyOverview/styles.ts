// Generated by Copilot
import { styled } from "@mui/material/styles";
import { Box } from "@mui/material";

export const PolicyCardWrapper = styled(Box)({
  background: "#fff",
  borderRadius: 12,
  border: "1px solid #f0f0f0",
  boxShadow: "0 1px 4px rgba(0,0,0,0.06)",
  marginBottom: 16,
  cursor: "pointer",
  transition: "all 0.2s",
  "&:hover": {
    boxShadow: "0 4px 16px rgba(0,0,0,0.10)",
    borderColor: "#dbeafe",
  },
});

export const PolicyNoBadge = styled(Box)({
  display: "flex",
  alignItems: "center",
  gap: 4,
  fontFamily: "monospace",
  fontSize: 11.5,
  color: "#6b7280",
  backgroundColor: "#f9fafb",
  border: "1px solid #e5e7eb",
  borderRadius: 6,
  padding: "4px 12px",
  cursor: "pointer",
  transition: "all 0.15s",
  "&:hover": {
    backgroundColor: "#eff6ff",
    color: "#2563eb",
    borderColor: "#bfdbfe",
  },
});

export const MemberActivityBtn = styled(Box)({
  display: "flex",
  alignItems: "center",
  gap: 6,
  fontSize: 12,
  color: "#9ca3af",
  backgroundColor: "#f9fafb",
  border: "1px solid #f0f0f0",
  borderRadius: 6,
  padding: "6px 12px",
  cursor: "pointer",
  "&:hover": { color: "#6b7280" },
});

export const ViewPolicyLink = styled(Box)({
  display: "flex",
  alignItems: "center",
  gap: 6,
  fontSize: 12,
  color: "#9ca3af",
  cursor: "pointer",
  "&:hover": { color: "#2563eb" },
});

export const CardDivider = styled(Box)({
  height: 1,
  backgroundColor: "#f3f4f6",
  margin: "0 32px",
});
