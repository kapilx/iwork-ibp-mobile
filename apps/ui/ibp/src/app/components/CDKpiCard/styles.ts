// Generated by Copilot
import { styled } from "@mui/material/styles";
import { Box, Typography } from "@mui/material";

export const CDKpiCardRoot = styled(Box)(({ theme }) => ({
  backgroundColor: theme.palette.background.paper,
  borderRadius: theme.shape.borderRadii.normal,
  border: `1px solid ${theme.palette.neutral.tableBorder}`,
  boxShadow: theme.shadows[1],
  padding: theme.spacing(2.5),
  display: "flex",
  flexDirection: "column",
  gap: theme.spacing(1.75),
}));

export const CDKpiIconWrap = styled(Box)<{ bgColor?: string }>(
  ({ bgColor }) => ({
    width: 36,
    height: 36,
    borderRadius: 12,
    backgroundColor: bgColor ?? "#dbeafe",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    flexShrink: 0,
  }),
);

export const CDKpiValue = styled(Typography)(({ theme }) => ({
  fontSize: theme.typography.fontSizes.lg,
  fontWeight: theme.typography.fontWeights.bold,
  color: theme.palette.neutral.mediumDark,
  lineHeight: 1.1,
}));

export const CDKpiSparkRow = styled(Box)(({ theme }) => ({
  display: "flex",
  alignItems: "center",
  gap: 0,
  borderTop: `1px solid ${theme.palette.neutral.tableBorder}`,
  paddingTop: theme.spacing(1),
}));

export const CDKpiSparkWrap = styled(Box)(() => ({
  width: "58%",
  paddingRight: 12,
}));

export const CDKpiDivider = styled(Box)(({ theme }) => ({
  width: 1,
  alignSelf: "stretch",
  backgroundColor: theme.palette.neutral.tableBorder,
  flexShrink: 0,
}));

export const CDKpiTrendWrap = styled(Box)(({ theme }) => ({
  flex: 1,
  paddingLeft: theme.spacing(1.5),
  display: "flex",
  flexDirection: "column",
  justifyContent: "center",
  gap: 2,
}));

export const CDKpiTrendText = styled(Typography)<{
  trendDir?: "up" | "down" | "neutral";
}>(({ theme, trendDir }) => ({
  fontSize: theme.typography.fontSizes.xs,
  fontWeight: theme.typography.fontWeights.semiBold,
  lineHeight: 1.2,
  display: "flex",
  alignItems: "center",
  gap: 2,
  color:
    trendDir === "up"
      ? theme.palette.neutral.green
      : trendDir === "down"
        ? theme.palette.text.error
        : theme.palette.text.yellow,
}));

export const CDKpiSubText = styled(Typography)(({ theme }) => ({
  fontSize: theme.typography.fontSizes.xxs,
  color: theme.palette.text.grey,
  lineHeight: 1.2,
}));

export const CDKpiLabel = styled(Typography)(({ theme }) => ({
  fontSize: theme.typography.fontSizes.xxxs,
  color: theme.palette.neutral.medium,
  lineHeight: 1.3,
}));
