// Generated by Copilot
import { styled } from "@mui/material/styles";
import { Box, Typography } from "@mui/material";
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import { ibpTheme as _theme } from "@ui/ui-lib";

// ─── Back Button ───────────────────────────────────────────────────────────────
export const BackButton = styled(Box)(({ theme }) => ({
    display: "flex",
    alignItems: "center",
    gap: theme.spacing(1),
    marginBottom: theme.spacing(2.5),
    backgroundColor: "transparent",
    border: "none",
    cursor: "pointer",
    padding: 0,
    color: theme.palette.text.grey,
    fontSize: theme.typography.fontSizes.xss,
    fontWeight: theme.typography.fontWeights.medium,
    "&:hover": {
        color: theme.palette.neutral.mediumDark,
    },
}));

// ─── Profile Header ────────────────────────────────────────────────────────────
export const ProfileAvatarBox = styled(Box)(({ theme }) => ({
    width: 56,
    height: 56,
    borderRadius: theme.shape.borderRadii.normal,
    background: `linear-gradient(135deg, ${theme.palette.secondary.selected}, #1E6FC4)`,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    flexShrink: 0,
}));

export const ProfileAvatarText = styled(Typography)(({ theme }) => ({
    fontSize: theme.typography.fontSizes.lg,
    fontWeight: theme.typography.fontWeights.bold,
    color: theme.palette.neutral.veryLight,
}));

export const ProfileName = styled(Typography)(({ theme }) => ({
    fontSize: theme.typography.fontSizes.xl,
    fontWeight: theme.typography.fontWeights.bold,
    color: theme.palette.neutral.mediumDark,
}));

export const ProfileSubline = styled(Typography)(({ theme }) => ({
    fontSize: theme.typography.fontSizes.xss,
    color: theme.palette.text.grey,
    marginTop: theme.spacing(0.25),
}));

export const ProfileEnrollChip = styled(Box, {
    shouldForwardProp: (p) => p !== "enrollStatus",
})<{ enrollStatus: string }>(({ theme, enrollStatus }) => {
    const map: Record<string, { bg: string; color: string }> = {
        Enrolled: {
            bg: theme.palette.background.tabsSelected,
            color: theme.palette.secondary.selected,
        },
        "Not Enrolled": {
            bg: theme.palette.chips.secondary + "18",
            color: theme.palette.text.error,
        },
        Pending: {
            bg: theme.palette.kpiColors.yellow,
            color: theme.palette.text.yellow,
        },
        Expired: {
            bg: theme.palette.chips.secondary + "18",
            color: theme.palette.text.error,
        },
    };
    const s = map[enrollStatus] ?? {
        bg: theme.palette.background.tableHeader,
        color: theme.palette.text.grey,
    };
    return {
        display: "inline-flex",
        alignItems: "center",
        gap: theme.spacing(0.75),
        padding: theme.spacing(0.625, 1.5),
        borderRadius: 20,
        backgroundColor: s.bg,
        color: s.color,
        fontSize: theme.typography.fontSizes.xs,
        fontWeight: theme.typography.fontWeights.semiBold,
    };
});

export const ProfilePolicyChip = styled(Box, {
    shouldForwardProp: (p) => p !== "policyStatus",
})<{ policyStatus: string }>(({ theme, policyStatus }) => {
    const map: Record<string, { bg: string; color: string }> = {
        Active: {
            bg: theme.palette.kpiColors.teal,
            color: theme.palette.neutral.green,
        },
        Pending: {
            bg: theme.palette.kpiColors.yellow,
            color: theme.palette.text.yellow,
        },
        Expired: {
            bg: theme.palette.chips.secondary + "18",
            color: theme.palette.text.error,
        },
    };
    const s = map[policyStatus] ?? {
        bg: theme.palette.background.tableHeader,
        color: theme.palette.text.grey,
    };
    return {
        display: "inline-flex",
        alignItems: "center",
        gap: theme.spacing(0.75),
        padding: theme.spacing(0.625, 1.5),
        borderRadius: 20,
        backgroundColor: s.bg,
        color: s.color,
        fontSize: theme.typography.fontSizes.xs,
        fontWeight: theme.typography.fontWeights.semiBold,
    };
});

export const ProfileMetaIconBox = styled(Box)(({ theme }) => ({
    width: 28,
    height: 28,
    borderRadius: theme.shape.borderRadii.small,
    backgroundColor: theme.palette.background.tableHeader,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    color: theme.palette.text.grey,
}));

export const ProfileMetaValue = styled(Typography)(({ theme }) => ({
    fontSize: theme.typography.fontSizes.xss,
    fontWeight: theme.typography.fontWeights.semiBold,
    color: theme.palette.neutral.mediumDark,
}));

export const ProfileMetaSub = styled(Typography)(({ theme }) => ({
    fontSize: "10px",
    color: theme.palette.text.grey,
}));

// ─── Personal Info / Enrollment cells ─────────────────────────────────────────
export const InfoGrid = styled(Box)(({ theme }) => ({
    display: "grid",
    gridTemplateColumns: "1fr 1fr",
    gap: theme.spacing(1.5),
    [theme.breakpoints.down("sm")]: {
        gridTemplateColumns: "1fr",
    },
}));

export const InfoCell = styled(Box)(({ theme }) => ({
    display: "flex",
    alignItems: "flex-start",
    gap: theme.spacing(1.5),
    padding: theme.spacing(1.5),
    borderRadius: theme.shape.borderRadii.medium,
    backgroundColor: theme.palette.background.tableHeader,
}));

export const InfoCellIconBox = styled(Box)(({ theme }) => ({
    width: 32,
    height: 32,
    borderRadius: theme.shape.borderRadii.small,
    backgroundColor: theme.palette.background.paper,
    border: `1px solid ${theme.palette.neutral.tableBorder}`,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    flexShrink: 0,
    color: theme.palette.text.grey,
}));

export const InfoCellLabel = styled(Typography)(({ theme }) => ({
    fontSize: "11px",
    color: theme.palette.text.grey,
    marginBottom: theme.spacing(0.25),
}));

export const InfoCellValue = styled(Typography)(({ theme }) => ({
    fontSize: theme.typography.fontSizes.xss,
    fontWeight: theme.typography.fontWeights.semiBold,
    color: theme.palette.neutral.mediumDark,
}));

// ─── Dependents ────────────────────────────────────────────────────────────────
export const DependentGrid = styled(Box)(({ theme }) => ({
    display: "grid",
    gridTemplateColumns: "1fr 1fr",
    gap: theme.spacing(2),
    [theme.breakpoints.down("sm")]: {
        gridTemplateColumns: "1fr",
    },
}));

export const DependentCard = styled(Box)(({ theme }) => ({
    border: `1px solid ${theme.palette.neutral.tableBorder}`,
    borderRadius: theme.shape.borderRadii.medium,
    padding: theme.spacing(2),
    transition: "border-color 0.15s ease",
    "&:hover": {
        borderColor: theme.palette.secondary.selected,
    },
}));

export const DependentCardAvatar = styled(Box)(({ theme }) => ({
    width: 36,
    height: 36,
    borderRadius: "50%",
    background: `linear-gradient(135deg, ${theme.palette.neutral.green}, #065F5A)`,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    flexShrink: 0,
}));

export const DependentCardAvatarText = styled(Typography)(({ theme }) => ({
    fontSize: theme.typography.fontSizes.xss,
    fontWeight: theme.typography.fontWeights.bold,
    color: theme.palette.neutral.veryLight,
}));

export const DependentRelBadge = styled(Box, {
    shouldForwardProp: (p) => p !== "relation",
})<{ relation: string }>(({ theme, relation }) => {
    const map: Record<string, { bg: string; color: string }> = {
        Spouse: { bg: "#FDF2F8", color: "#DB2777" },
        Child: {
            bg: theme.palette.background.tabsSelected,
            color: theme.palette.secondary.selected,
        },
        Parent: {
            bg: theme.palette.kpiColors.purple,
            color: "#7C3AED",
        },
    };
    const s = map[relation] ?? {
        bg: theme.palette.background.tableHeader,
        color: theme.palette.text.grey,
    };
    return {
        display: "inline-block",
        padding: theme.spacing(0.25, 1.25),
        borderRadius: 20,
        fontSize: "11px",
        fontWeight: theme.typography.fontWeights.semiBold,
        backgroundColor: s.bg,
        color: s.color,
    };
});

export const DependentDataCell = styled(Box)(({ theme }) => ({
    backgroundColor: theme.palette.background.tableHeader,
    borderRadius: theme.shape.borderRadii.small,
    padding: theme.spacing(1.25),
}));

export const DependentDataLabel = styled(Typography)(({ theme }) => ({
    fontSize: "10px",
    color: theme.palette.text.grey,
}));

export const DependentDataValue = styled(Typography)(({ theme }) => ({
    fontSize: theme.typography.fontSizes.xss,
    fontWeight: theme.typography.fontWeights.semiBold,
    color: theme.palette.neutral.mediumDark,
}));

export const DependentCovValue = styled(Typography)(({ theme }) => ({
    fontSize: theme.typography.fontSizes.xss,
    fontWeight: theme.typography.fontWeights.semiBold,
    color: theme.palette.neutral.green,
}));

// ─── Claims ────────────────────────────────────────────────────────────────────
export const ClaimsTableWrap = styled(Box)(({ theme }) => ({
    overflowX: "auto",
    borderRadius: theme.shape.borderRadii.medium,
    border: `1px solid ${theme.palette.neutral.tableBorder}`,
}));

export const ClaimStatusBadge = styled(Box, {
    shouldForwardProp: (p) => p !== "claimStatus",
})<{ claimStatus?: string }>(({ theme, claimStatus }) => {
    const map: Record<string, { bg: string; color: string }> = {
        Paid: { bg: theme.palette.background.tabsSelected, color: theme.palette.neutral.green },
        Outstanding: { bg: theme.palette.kpiColors.yellow, color: theme.palette.text.yellow },
        Pending: { bg: theme.palette.kpiColors.yellow, color: theme.palette.text.yellow },
        Rejected: { bg: theme.palette.chips.secondary + "18", color: theme.palette.text.error },
        Denied: { bg: theme.palette.chips.secondary + "18", color: theme.palette.text.error },
        Closed: {
            bg: theme.palette.background.tableHeader,
            color: theme.palette.secondary.selected,
        },
    };
    const s = map[claimStatus ?? ""] ?? {
        bg: theme.palette.background.tableHeader,
        color: theme.palette.text.grey,
    };
    return {
        display: "inline-flex",
        alignItems: "center",
        padding: theme.spacing(0.25, 1.25),
        borderRadius: 20,
        backgroundColor: s.bg,
        color: s.color,
        fontSize: "11px",
        fontWeight: theme.typography.fontWeights.semiBold,
    };
});

export const ClaimTypeBadge = styled(Box, {
    shouldForwardProp: (p) => p !== "claimType",
})<{ claimType?: "Cashless" | "Reimbursement" }>(({ theme, claimType }) => ({
    display: "inline-block",
    padding: theme.spacing(0.25, 1.25),
    borderRadius: 20,
    fontSize: "11px",
    fontWeight: theme.typography.fontWeights.semiBold,
    backgroundColor:
        claimType === "Cashless"
            ? theme.palette.kpiColors.teal
            : theme.palette.kpiColors.purple,
    color:
        claimType === "Cashless"
            ? theme.palette.neutral.green
            : "#7C3AED",
}));

// ─── Documents ─────────────────────────────────────────────────────────────────
export const DocRow = styled(Box)(({ theme }) => ({
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    padding: theme.spacing(2),
    border: `1px solid ${theme.palette.neutral.tableBorder}`,
    borderRadius: theme.shape.borderRadii.medium,
    cursor: "default",
    transition: "all 0.12s ease",
    "&:hover": {
        borderColor: theme.palette.secondary.selected,
        backgroundColor: theme.palette.background.tabsSelected,
    },
}));

export const DocIconBox = styled(Box)(({ theme }) => ({
    width: 36,
    height: 36,
    borderRadius: theme.shape.borderRadii.small,
    backgroundColor: theme.palette.chips.secondary + "18",
    border: `1px solid ${theme.palette.chips.secondary}30`,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    flexShrink: 0,
    color: theme.palette.text.error,
}));

export const DocName = styled(Typography)(({ theme }) => ({
    fontSize: theme.typography.fontSizes.xss,
    fontWeight: theme.typography.fontWeights.medium,
    color: theme.palette.neutral.mediumDark,
}));

export const DocMeta = styled(Typography)(({ theme }) => ({
    fontSize: "10px",
    color: theme.palette.text.grey,
}));

export const DocTypeBadge = styled(Box, {
    shouldForwardProp: (p) => p !== "docType",
})<{ docType?: string }>(({ theme, docType }) => {
    const map: Record<string, { bg: string; color: string }> = {
        Policy: {
            bg: theme.palette.background.tabsSelected,
            color: theme.palette.secondary.selected,
        },
        KYC: {
            bg: theme.palette.kpiColors.purple,
            color: "#7C3AED",
        },
        Claim: {
            bg: theme.palette.background.tabsSelected,
            color: theme.palette.neutral.green,
        },
        Enrollment: {
            bg: theme.palette.kpiColors.yellow,
            color: theme.palette.text.yellow,
        },
    };
    const s = map[docType ?? ""] ?? {
        bg: theme.palette.background.tableHeader,
        color: theme.palette.text.grey,
    };
    return {
        padding: theme.spacing(0.25, 1.25),
        borderRadius: 20,
        backgroundColor: s.bg,
        color: s.color,
        fontSize: "11px",
        fontWeight: theme.typography.fontWeights.semiBold,
    };
});

export const DocDownloadBtn = styled(Box)(({ theme }) => ({
    padding: theme.spacing(0.75),
    borderRadius: theme.shape.borderRadii.small,
    backgroundColor: "transparent",
    border: "none",
    cursor: "pointer",
    color: theme.palette.text.grey,
    display: "flex",
    alignItems: "center",
    "&:hover": {
        backgroundColor: theme.palette.background.tabsSelected,
        color: theme.palette.secondary.selected,
    },
}));

// ─── Activity Log ──────────────────────────────────────────────────────────────
export const ActivityTimelineRoot = styled(Box)(() => ({
    position: "relative",
}));

export const ActivityTimelineLine = styled(Box)(({ theme }) => ({
    position: "absolute",
    left: 15,
    top: 0,
    bottom: 0,
    width: 2,
    backgroundColor: theme.palette.neutral.tableBorder,
}));

export const ActivityBullet = styled(Box, {
    shouldForwardProp: (p) => p !== "dotColor",
})<{ dotColor?: string }>(({ dotColor, theme }) => ({
    width: 30,
    height: 30,
    borderRadius: "50%",
    backgroundColor: (dotColor ?? theme.palette.secondary.selected) + "18",
    border: `2px solid ${theme.palette.background.paper}`,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    flexShrink: 0,
    zIndex: 1,
    color: dotColor ?? theme.palette.secondary.selected,
}));

export const ActivityText = styled(Typography)(({ theme }) => ({
    fontSize: theme.typography.fontSizes.xss,
    fontWeight: theme.typography.fontWeights.medium,
    color: theme.palette.neutral.mediumDark,
}));

export const ActivityMeta = styled(Typography)(({ theme }) => ({
    fontSize: "11px",
    color: theme.palette.text.grey,
    marginTop: theme.spacing(0.25),
}));

// ─── Enrollment Activity (enrollment history sub-section) ─────────────────────
export const EnrollmentActivityRow = styled(Box)(({ theme }) => ({
    display: "flex",
    alignItems: "flex-start",
    gap: theme.spacing(1.5),
    padding: theme.spacing(1.5),
    borderRadius: theme.shape.borderRadii.medium,
    backgroundColor: theme.palette.background.tabsSelected,
    color: theme.palette.secondary.selected,
}));

export const EnrollmentActivityText = styled(Typography)(({ theme }) => ({
    fontSize: theme.typography.fontSizes.xs,
    fontWeight: theme.typography.fontWeights.medium,
    color: theme.palette.secondary.selected,
}));

export const EnrollmentActivityMeta = styled(Typography)(({ theme }) => ({
    fontSize: "10px",
    color: theme.palette.secondary.selected,
    marginTop: theme.spacing(0.25),
    opacity: 0.8,
}));

// ─── Section header (for tabs with just a title + optional action) ────────────
export const TabSectionTitle = styled(Typography)(({ theme }) => ({
    fontSize: theme.typography.fontSizes.xss,
    fontWeight: theme.typography.fontWeights.semiBold,
    color: theme.palette.neutral.mediumDark,
    marginBottom: theme.spacing(2.5),
}));
